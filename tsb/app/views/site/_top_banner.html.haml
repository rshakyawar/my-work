%section.secTwo
  #logoOne
    %img{:alt => "Logo One", :src => "#{@product.logo_one.url(:medium)}"}/
  #topBanner
    .mapwrapper2
      #viewport2
        .current-map-layer{:style => "background: url('#{@product.top_banner.url(:medium)}') no-repeat scroll 0% 0% transparent; width: 441px; height: 52px;"}
        %div{:style => "height: 100px; width: 550px;"}
          = image_tag @product.top_banner.url(:medium)
          .mapcontent
      #viewport{:style => "display: none;"}
        %img{:alt => "Top Banner", :src => "/images/2.gif"}/
  #bgImgBlock{:style=>"background-image:url('#{@product.background.url(:medium)}');"}
    %p Background Image
    #versusBlock
      #versusLeft.versusLeft_set
        %img{:alt => "Versus Left", :src => "#{@product.versus_left.url(:medium)}"}/
      #versusRight.versusRight_set
        %img{:alt => "Versus Right", :src => "#{@product.versus_right.url(:medium)}"}/
  #bottomBannerBlock{:style=>"background-color:#{@product.bottom_banner_color}"}
    #bannerTxt
      #quizBlock_bg QUESTION
#logo2Block
  #logoTwo
    %img{:alt => "Logo Two", :src => "#{@product.logo_two.url(:medium)}"}/
:javascript
  $(document).ready(function() {setPanelControls('#viewport2');	});